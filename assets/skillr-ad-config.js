/* Keep false until the SkillrHub AdSense application is approved. */
window.SkillrAdConfig = Object.freeze({
  enabled: false,
  client: "ca-pub-7734963540104771",
  slots: Object.freeze({
    homeworkEnd: ""
  })
});

(()=>{"use strict";
const e=s=>String(s??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
const valid=m=>{const p=m?.parameters||{},items=p.items||p.labels;if(!Array.isArray(items)||items.length<2||items.length>8)throw new Error(`Invalid English model items for ${m?.id}`);if(!Number.isInteger(p.focusIndex)||p.focusIndex<-1||p.focusIndex>=items.length)throw new Error(`Invalid focusIndex for ${m?.id}`);return {...p,items}};
const active=(p,i)=>p.focusIndex===i?" is-focus":"";
const caption=m=>`<figcaption>${e(m.purpose)}</figcaption>`;
const registerScale=m=>{const p=valid(m);return `<figure class="english-model model-register" role="img" aria-label="${e(m.accessibleDescription)}"><div class="register-scale">${p.items.map((x,i)=>`<section class="${active(p,i)}"><small>${e(x.label)}</small><b>${e(x.phrase)}</b><span>${e(x.effect)}</span></section>`).join("")}</div><p class="model-context">${e(p.context)}</p>${caption(m)}</figure>`};
const identityWeb=m=>{const p=valid(m);return `<figure class="english-model model-identity" role="img" aria-label="${e(m.accessibleDescription)}"><div class="identity-web"><div class="identity-centre">${e(p.centre||"group identity")}</div>${p.items.map((x,i)=>`<section class="identity-node n${i}${active(p,i)}"><small>${e(x.label)}</small><b>${e(x.example)}</b></section>`).join("")}</div><p class="model-context">${e(p.context)}</p>${caption(m)}</figure>`};
const countryMap=m=>{const p=valid(m);return `<figure class="english-model model-country" role="img" aria-label="${e(m.accessibleDescription)}"><div class="country-panel"><div class="map-card"><span class="map-shape a"></span><span class="map-shape b"></span><span class="map-shape c"></span><b>Language names</b><small>show relationships to Country/Place</small></div><div class="country-list">${p.items.map((x,i)=>`<section class="${active(p,i)}"><small>${e(x.label)}</small><b>${e(x.example)}</b><span>${e(x.note)}</span></section>`).join("")}</div></div><p class="model-context">${e(p.context)}</p>${caption(m)}</figure>`};
const textCompare=m=>{const p=valid(m);return `<figure class="english-model model-compare" role="img" aria-label="${e(m.accessibleDescription)}"><div class="text-compare">${p.items.map((x,i)=>`<section class="${active(p,i)}"><small>${e(x.label)}</small><p>${e(x.text)}</p><b>${e(x.effect)}</b></section>`).join("")}</div><p class="model-context">${e(p.context)}</p>${caption(m)}</figure>`};
const render=m=>({registerScale,identityWeb,countryMap,textCompare}[m.component]||registerScale)(m);
window.SkillrUpperEnglishVisuals={render};
})();

# Batch 002 — IXL depth checkpoint

Status: redone for AC9M5N05–AC9M5N08; pending remote publication.

## IXL evidence

- AC9M5N05 — `add-fractions-with-unlike-denominators`: Learn by example solved `2/9 + 1/3` by selecting a related common denominator, renaming `1/3` as `3/9`, then adding numerators to obtain `5/9`. Depth: denominator relationship → equivalent fraction → operation → check.
- AC9M5N06 — `multiply-two-digit-numbers-by-two-digit-numbers-using-partial-products`: Learn by example solved `91 × 24` with `90+1` and `20+4`, four rectangle products, then `2184`. Depth: area model → place-value partial products → recombination.
- AC9M5N07 — `divide-using-partial-quotients`: Learn by example solved `57 ÷ 3` as `30 ÷ 3 + 27 ÷ 3 = 10+9 = 19`. Depth: convenient multiples → subtraction → add partial quotients → verify.
- AC9M5N08 — `estimate-quotients`: Learn by example used the compatible number `30 000 ÷ 6 = 5000` to compare with `8055`. Depth: choose useful estimate → predict scale/inequality → explain reasonableness.

## Curriculum and authoring notes

The Australian Curriculum descriptors require related denominators, efficient multiplication strategies, remainder interpretation, and context-appropriate reasonableness checks. The original opening questions overused thin repeated contexts and did not expose the reasoning structure. Revised representative items now require explanations, partial products, remainder meaning, or estimate justification while preserving the existing 24-practice/16-test structure.

Misconceptions targeted: adding denominators; losing place value in partial products; treating a remainder as the quotient or ignoring it without context; and accepting an answer with the wrong scale. Practice and Test banks remain separate with no intentional exact duplicate.

Shared files changed: four Topic Guides, four Classroom Views, and the four Practice question banks. No worksheets changed.

Next adjacent pending batch: AC9M5N09 onward (confirm exact pending codes from the repository after this batch is published).
